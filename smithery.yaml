name: fiberforge
version: 1.0.0
description: "Deterministic, schema-driven Go Fiber project scaffolder with an embedded MCP server"
author: "Vaibhav Pathak"
license: "MIT"

startCommand:
  type: stdio
  command: npx
  args:
    - -y
    - fiberforge-cli
    - serve
